import { WidgetHost } from './widget-host';

describe('WidgetHost', () => {
  it('should create an instance', () => {
    const directive = new WidgetHost();
    expect(directive).toBeTruthy();
  });
});
